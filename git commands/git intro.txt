git clone  https://github.com/aryamol2002/sample.git

git and github

alternatives : AWS,bitbucket,gitlab

git add >>> add the file to the staging alternatives (file is being tracked by git)

git comit -m "message" >>> to commit the changes in the tracked file

git push origin main >>>> pushing the file to the main branch in the github repository

git add filename

git add .
